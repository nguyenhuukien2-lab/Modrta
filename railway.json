{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --include=dev && npx prisma generate --schema=backend/prisma/schema.prisma && cd backend && npm install --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
